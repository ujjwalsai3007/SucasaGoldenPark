<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/item_background"
    android:layout_margin="8dp">

    <!-- House Number -->
    <TextView
        android:id="@+id/houseNumberTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="House Number: G5"
        android:textStyle="bold"
        android:textSize="16sp"
        android:layout_marginBottom="4dp" />

    <!-- Issue Title -->
    <TextView
        android:id="@+id/issueTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Issue Title"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_marginBottom="8dp" />

    <!-- Issue Description -->
    <TextView
        android:id="@+id/issueDescriptionTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Issue Description: Description text goes here..."
        android:textSize="14sp" />

    <Button
        android:id="@+id/deleteIssueButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@android:color/holo_red_dark"
        android:text="Delete"
        android:textColor="@android:color/white"
        android:textSize="10dp" />

</LinearLayout>